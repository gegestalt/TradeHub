name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy (placeholder)
    runs-on: ubuntu-latest
    needs: []

    steps:
      - uses: actions/checkout@v4

      - name: Placeholder
        run: echo "Deployment not yet configured. Wire up your target platform here."
